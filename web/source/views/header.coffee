define [
    'backbone',
    'store'
    'dropMenu',
], (Backbone, Store, dropMenu) ->
    'use strict'

    HeaderView = Backbone.View.extend
        initialize: ->

        render: ->

        remove: ->
            @_super('remove')


