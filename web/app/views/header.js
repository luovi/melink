(function() {
  define(['backbone', 'store', 'dropMenu'], function(Backbone, Store, dropMenu) {
    'use strict';
    var HeaderView;
    return HeaderView = Backbone.View.extend({
      initialize: function() {},
      render: function() {},
      remove: function() {
        return this._super('remove');
      }
    });
  });

}).call(this);
